import React from 'react'
import { Layout } from '../../components'

const Orders = () => {
    return (
        <Layout title={`Orders`}>
            
        </Layout>
    )
}

export default Orders
